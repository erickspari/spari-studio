# Formspree form ID (https://formspree.io) — create a form, paste its ID here.
# Without it, the contact form falls back to a graceful error message and
# still surfaces the mailto link as a backup.
NEXT_PUBLIC_FORMSPREE_ID=
